{% extends 'adminbase.html.twig' %}

{% block title %}New Product
{% endblock %}

{% block body %}
	<div class="main-panel">
		<div class="content-wrapper">
			<div class="col-lg-12 grid-margin stretch-card">
				<div class="card">
					<div class="card-body">
						<h4 class="card-title">
							New Product</h4>
						<button>
							<a href="{{ path('app_admin_category_new') }}">Add
							</a>
						</button>

						<div class="table-responsive"></div>
						<div class="card">
							<div class="card-body">

								<div class="table-responsive">

									{{ include('admin/product/_form.html.twig') }}

									<a href="{{ path('app_admin_product_index') }}" class="btn btn-success">back to list</a>
								</div>
							</div>
						</div>
					</div>
				</div>
			</div>
		{% endblock %}
